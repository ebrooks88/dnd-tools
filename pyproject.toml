[project]
name = "dnd-tools"
version = "0.1.0"
description = "Small Python tools for DND."
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["dnd"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"